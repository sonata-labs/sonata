// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: api/v1/services.proto

package v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/sonata-labs/sonata/gen/api/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// CoreName is the fully-qualified name of the Core service.
	CoreName = "api.v1.Core"
	// StorageName is the fully-qualified name of the Storage service.
	StorageName = "api.v1.Storage"
	// SystemName is the fully-qualified name of the System service.
	SystemName = "api.v1.System"
	// P2PName is the fully-qualified name of the P2P service.
	P2PName = "api.v1.P2P"
	// DDEXName is the fully-qualified name of the DDEX service.
	DDEXName = "api.v1.DDEX"
	// CompositionName is the fully-qualified name of the Composition service.
	CompositionName = "api.v1.Composition"
	// AccountName is the fully-qualified name of the Account service.
	AccountName = "api.v1.Account"
	// ValidatorName is the fully-qualified name of the Validator service.
	ValidatorName = "api.v1.Validator"
	// AppStoreName is the fully-qualified name of the AppStore service.
	AppStoreName = "api.v1.AppStore"
	// LocalStoreName is the fully-qualified name of the LocalStore service.
	LocalStoreName = "api.v1.LocalStore"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// CorePingProcedure is the fully-qualified name of the Core's Ping RPC.
	CorePingProcedure = "/api.v1.Core/Ping"
	// StoragePingProcedure is the fully-qualified name of the Storage's Ping RPC.
	StoragePingProcedure = "/api.v1.Storage/Ping"
	// SystemPingProcedure is the fully-qualified name of the System's Ping RPC.
	SystemPingProcedure = "/api.v1.System/Ping"
	// P2PPingProcedure is the fully-qualified name of the P2P's Ping RPC.
	P2PPingProcedure = "/api.v1.P2P/Ping"
	// DDEXPingProcedure is the fully-qualified name of the DDEX's Ping RPC.
	DDEXPingProcedure = "/api.v1.DDEX/Ping"
	// CompositionPingProcedure is the fully-qualified name of the Composition's Ping RPC.
	CompositionPingProcedure = "/api.v1.Composition/Ping"
	// AccountPingProcedure is the fully-qualified name of the Account's Ping RPC.
	AccountPingProcedure = "/api.v1.Account/Ping"
	// ValidatorPingProcedure is the fully-qualified name of the Validator's Ping RPC.
	ValidatorPingProcedure = "/api.v1.Validator/Ping"
	// AppStorePingProcedure is the fully-qualified name of the AppStore's Ping RPC.
	AppStorePingProcedure = "/api.v1.AppStore/Ping"
	// LocalStorePingProcedure is the fully-qualified name of the LocalStore's Ping RPC.
	LocalStorePingProcedure = "/api.v1.LocalStore/Ping"
)

// CoreClient is a client for the api.v1.Core service.
type CoreClient interface {
	Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error)
}

// NewCoreClient constructs a client for the api.v1.Core service. By default, it uses the Connect
// protocol with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed
// requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewCoreClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) CoreClient {
	baseURL = strings.TrimRight(baseURL, "/")
	coreMethods := v1.File_api_v1_services_proto.Services().ByName("Core").Methods()
	return &coreClient{
		ping: connect.NewClient[v1.PingRequest, v1.PingResponse](
			httpClient,
			baseURL+CorePingProcedure,
			connect.WithSchema(coreMethods.ByName("Ping")),
			connect.WithClientOptions(opts...),
		),
	}
}

// coreClient implements CoreClient.
type coreClient struct {
	ping *connect.Client[v1.PingRequest, v1.PingResponse]
}

// Ping calls api.v1.Core.Ping.
func (c *coreClient) Ping(ctx context.Context, req *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error) {
	return c.ping.CallUnary(ctx, req)
}

// CoreHandler is an implementation of the api.v1.Core service.
type CoreHandler interface {
	Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error)
}

// NewCoreHandler builds an HTTP handler from the service implementation. It returns the path on
// which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewCoreHandler(svc CoreHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	coreMethods := v1.File_api_v1_services_proto.Services().ByName("Core").Methods()
	corePingHandler := connect.NewUnaryHandler(
		CorePingProcedure,
		svc.Ping,
		connect.WithSchema(coreMethods.ByName("Ping")),
		connect.WithHandlerOptions(opts...),
	)
	return "/api.v1.Core/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case CorePingProcedure:
			corePingHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedCoreHandler returns CodeUnimplemented from all methods.
type UnimplementedCoreHandler struct{}

func (UnimplementedCoreHandler) Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("api.v1.Core.Ping is not implemented"))
}

// StorageClient is a client for the api.v1.Storage service.
type StorageClient interface {
	Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error)
}

// NewStorageClient constructs a client for the api.v1.Storage service. By default, it uses the
// Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewStorageClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) StorageClient {
	baseURL = strings.TrimRight(baseURL, "/")
	storageMethods := v1.File_api_v1_services_proto.Services().ByName("Storage").Methods()
	return &storageClient{
		ping: connect.NewClient[v1.PingRequest, v1.PingResponse](
			httpClient,
			baseURL+StoragePingProcedure,
			connect.WithSchema(storageMethods.ByName("Ping")),
			connect.WithClientOptions(opts...),
		),
	}
}

// storageClient implements StorageClient.
type storageClient struct {
	ping *connect.Client[v1.PingRequest, v1.PingResponse]
}

// Ping calls api.v1.Storage.Ping.
func (c *storageClient) Ping(ctx context.Context, req *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error) {
	return c.ping.CallUnary(ctx, req)
}

// StorageHandler is an implementation of the api.v1.Storage service.
type StorageHandler interface {
	Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error)
}

// NewStorageHandler builds an HTTP handler from the service implementation. It returns the path on
// which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewStorageHandler(svc StorageHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	storageMethods := v1.File_api_v1_services_proto.Services().ByName("Storage").Methods()
	storagePingHandler := connect.NewUnaryHandler(
		StoragePingProcedure,
		svc.Ping,
		connect.WithSchema(storageMethods.ByName("Ping")),
		connect.WithHandlerOptions(opts...),
	)
	return "/api.v1.Storage/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case StoragePingProcedure:
			storagePingHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedStorageHandler returns CodeUnimplemented from all methods.
type UnimplementedStorageHandler struct{}

func (UnimplementedStorageHandler) Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("api.v1.Storage.Ping is not implemented"))
}

// SystemClient is a client for the api.v1.System service.
type SystemClient interface {
	Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error)
}

// NewSystemClient constructs a client for the api.v1.System service. By default, it uses the
// Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSystemClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SystemClient {
	baseURL = strings.TrimRight(baseURL, "/")
	systemMethods := v1.File_api_v1_services_proto.Services().ByName("System").Methods()
	return &systemClient{
		ping: connect.NewClient[v1.PingRequest, v1.PingResponse](
			httpClient,
			baseURL+SystemPingProcedure,
			connect.WithSchema(systemMethods.ByName("Ping")),
			connect.WithClientOptions(opts...),
		),
	}
}

// systemClient implements SystemClient.
type systemClient struct {
	ping *connect.Client[v1.PingRequest, v1.PingResponse]
}

// Ping calls api.v1.System.Ping.
func (c *systemClient) Ping(ctx context.Context, req *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error) {
	return c.ping.CallUnary(ctx, req)
}

// SystemHandler is an implementation of the api.v1.System service.
type SystemHandler interface {
	Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error)
}

// NewSystemHandler builds an HTTP handler from the service implementation. It returns the path on
// which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSystemHandler(svc SystemHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	systemMethods := v1.File_api_v1_services_proto.Services().ByName("System").Methods()
	systemPingHandler := connect.NewUnaryHandler(
		SystemPingProcedure,
		svc.Ping,
		connect.WithSchema(systemMethods.ByName("Ping")),
		connect.WithHandlerOptions(opts...),
	)
	return "/api.v1.System/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SystemPingProcedure:
			systemPingHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSystemHandler returns CodeUnimplemented from all methods.
type UnimplementedSystemHandler struct{}

func (UnimplementedSystemHandler) Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("api.v1.System.Ping is not implemented"))
}

// P2PClient is a client for the api.v1.P2P service.
type P2PClient interface {
	Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error)
}

// NewP2PClient constructs a client for the api.v1.P2P service. By default, it uses the Connect
// protocol with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed
// requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewP2PClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) P2PClient {
	baseURL = strings.TrimRight(baseURL, "/")
	p2PMethods := v1.File_api_v1_services_proto.Services().ByName("P2P").Methods()
	return &p2PClient{
		ping: connect.NewClient[v1.PingRequest, v1.PingResponse](
			httpClient,
			baseURL+P2PPingProcedure,
			connect.WithSchema(p2PMethods.ByName("Ping")),
			connect.WithClientOptions(opts...),
		),
	}
}

// p2PClient implements P2PClient.
type p2PClient struct {
	ping *connect.Client[v1.PingRequest, v1.PingResponse]
}

// Ping calls api.v1.P2P.Ping.
func (c *p2PClient) Ping(ctx context.Context, req *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error) {
	return c.ping.CallUnary(ctx, req)
}

// P2PHandler is an implementation of the api.v1.P2P service.
type P2PHandler interface {
	Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error)
}

// NewP2PHandler builds an HTTP handler from the service implementation. It returns the path on
// which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewP2PHandler(svc P2PHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	p2PMethods := v1.File_api_v1_services_proto.Services().ByName("P2P").Methods()
	p2PPingHandler := connect.NewUnaryHandler(
		P2PPingProcedure,
		svc.Ping,
		connect.WithSchema(p2PMethods.ByName("Ping")),
		connect.WithHandlerOptions(opts...),
	)
	return "/api.v1.P2P/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case P2PPingProcedure:
			p2PPingHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedP2PHandler returns CodeUnimplemented from all methods.
type UnimplementedP2PHandler struct{}

func (UnimplementedP2PHandler) Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("api.v1.P2P.Ping is not implemented"))
}

// DDEXClient is a client for the api.v1.DDEX service.
type DDEXClient interface {
	Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error)
}

// NewDDEXClient constructs a client for the api.v1.DDEX service. By default, it uses the Connect
// protocol with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed
// requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewDDEXClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) DDEXClient {
	baseURL = strings.TrimRight(baseURL, "/")
	dDEXMethods := v1.File_api_v1_services_proto.Services().ByName("DDEX").Methods()
	return &dDEXClient{
		ping: connect.NewClient[v1.PingRequest, v1.PingResponse](
			httpClient,
			baseURL+DDEXPingProcedure,
			connect.WithSchema(dDEXMethods.ByName("Ping")),
			connect.WithClientOptions(opts...),
		),
	}
}

// dDEXClient implements DDEXClient.
type dDEXClient struct {
	ping *connect.Client[v1.PingRequest, v1.PingResponse]
}

// Ping calls api.v1.DDEX.Ping.
func (c *dDEXClient) Ping(ctx context.Context, req *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error) {
	return c.ping.CallUnary(ctx, req)
}

// DDEXHandler is an implementation of the api.v1.DDEX service.
type DDEXHandler interface {
	Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error)
}

// NewDDEXHandler builds an HTTP handler from the service implementation. It returns the path on
// which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewDDEXHandler(svc DDEXHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	dDEXMethods := v1.File_api_v1_services_proto.Services().ByName("DDEX").Methods()
	dDEXPingHandler := connect.NewUnaryHandler(
		DDEXPingProcedure,
		svc.Ping,
		connect.WithSchema(dDEXMethods.ByName("Ping")),
		connect.WithHandlerOptions(opts...),
	)
	return "/api.v1.DDEX/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case DDEXPingProcedure:
			dDEXPingHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedDDEXHandler returns CodeUnimplemented from all methods.
type UnimplementedDDEXHandler struct{}

func (UnimplementedDDEXHandler) Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("api.v1.DDEX.Ping is not implemented"))
}

// CompositionClient is a client for the api.v1.Composition service.
type CompositionClient interface {
	Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error)
}

// NewCompositionClient constructs a client for the api.v1.Composition service. By default, it uses
// the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewCompositionClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) CompositionClient {
	baseURL = strings.TrimRight(baseURL, "/")
	compositionMethods := v1.File_api_v1_services_proto.Services().ByName("Composition").Methods()
	return &compositionClient{
		ping: connect.NewClient[v1.PingRequest, v1.PingResponse](
			httpClient,
			baseURL+CompositionPingProcedure,
			connect.WithSchema(compositionMethods.ByName("Ping")),
			connect.WithClientOptions(opts...),
		),
	}
}

// compositionClient implements CompositionClient.
type compositionClient struct {
	ping *connect.Client[v1.PingRequest, v1.PingResponse]
}

// Ping calls api.v1.Composition.Ping.
func (c *compositionClient) Ping(ctx context.Context, req *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error) {
	return c.ping.CallUnary(ctx, req)
}

// CompositionHandler is an implementation of the api.v1.Composition service.
type CompositionHandler interface {
	Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error)
}

// NewCompositionHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewCompositionHandler(svc CompositionHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	compositionMethods := v1.File_api_v1_services_proto.Services().ByName("Composition").Methods()
	compositionPingHandler := connect.NewUnaryHandler(
		CompositionPingProcedure,
		svc.Ping,
		connect.WithSchema(compositionMethods.ByName("Ping")),
		connect.WithHandlerOptions(opts...),
	)
	return "/api.v1.Composition/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case CompositionPingProcedure:
			compositionPingHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedCompositionHandler returns CodeUnimplemented from all methods.
type UnimplementedCompositionHandler struct{}

func (UnimplementedCompositionHandler) Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("api.v1.Composition.Ping is not implemented"))
}

// AccountClient is a client for the api.v1.Account service.
type AccountClient interface {
	Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error)
}

// NewAccountClient constructs a client for the api.v1.Account service. By default, it uses the
// Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewAccountClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) AccountClient {
	baseURL = strings.TrimRight(baseURL, "/")
	accountMethods := v1.File_api_v1_services_proto.Services().ByName("Account").Methods()
	return &accountClient{
		ping: connect.NewClient[v1.PingRequest, v1.PingResponse](
			httpClient,
			baseURL+AccountPingProcedure,
			connect.WithSchema(accountMethods.ByName("Ping")),
			connect.WithClientOptions(opts...),
		),
	}
}

// accountClient implements AccountClient.
type accountClient struct {
	ping *connect.Client[v1.PingRequest, v1.PingResponse]
}

// Ping calls api.v1.Account.Ping.
func (c *accountClient) Ping(ctx context.Context, req *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error) {
	return c.ping.CallUnary(ctx, req)
}

// AccountHandler is an implementation of the api.v1.Account service.
type AccountHandler interface {
	Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error)
}

// NewAccountHandler builds an HTTP handler from the service implementation. It returns the path on
// which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewAccountHandler(svc AccountHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	accountMethods := v1.File_api_v1_services_proto.Services().ByName("Account").Methods()
	accountPingHandler := connect.NewUnaryHandler(
		AccountPingProcedure,
		svc.Ping,
		connect.WithSchema(accountMethods.ByName("Ping")),
		connect.WithHandlerOptions(opts...),
	)
	return "/api.v1.Account/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case AccountPingProcedure:
			accountPingHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedAccountHandler returns CodeUnimplemented from all methods.
type UnimplementedAccountHandler struct{}

func (UnimplementedAccountHandler) Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("api.v1.Account.Ping is not implemented"))
}

// ValidatorClient is a client for the api.v1.Validator service.
type ValidatorClient interface {
	Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error)
}

// NewValidatorClient constructs a client for the api.v1.Validator service. By default, it uses the
// Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewValidatorClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ValidatorClient {
	baseURL = strings.TrimRight(baseURL, "/")
	validatorMethods := v1.File_api_v1_services_proto.Services().ByName("Validator").Methods()
	return &validatorClient{
		ping: connect.NewClient[v1.PingRequest, v1.PingResponse](
			httpClient,
			baseURL+ValidatorPingProcedure,
			connect.WithSchema(validatorMethods.ByName("Ping")),
			connect.WithClientOptions(opts...),
		),
	}
}

// validatorClient implements ValidatorClient.
type validatorClient struct {
	ping *connect.Client[v1.PingRequest, v1.PingResponse]
}

// Ping calls api.v1.Validator.Ping.
func (c *validatorClient) Ping(ctx context.Context, req *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error) {
	return c.ping.CallUnary(ctx, req)
}

// ValidatorHandler is an implementation of the api.v1.Validator service.
type ValidatorHandler interface {
	Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error)
}

// NewValidatorHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewValidatorHandler(svc ValidatorHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	validatorMethods := v1.File_api_v1_services_proto.Services().ByName("Validator").Methods()
	validatorPingHandler := connect.NewUnaryHandler(
		ValidatorPingProcedure,
		svc.Ping,
		connect.WithSchema(validatorMethods.ByName("Ping")),
		connect.WithHandlerOptions(opts...),
	)
	return "/api.v1.Validator/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ValidatorPingProcedure:
			validatorPingHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedValidatorHandler returns CodeUnimplemented from all methods.
type UnimplementedValidatorHandler struct{}

func (UnimplementedValidatorHandler) Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("api.v1.Validator.Ping is not implemented"))
}

// AppStoreClient is a client for the api.v1.AppStore service.
type AppStoreClient interface {
	Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error)
}

// NewAppStoreClient constructs a client for the api.v1.AppStore service. By default, it uses the
// Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewAppStoreClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) AppStoreClient {
	baseURL = strings.TrimRight(baseURL, "/")
	appStoreMethods := v1.File_api_v1_services_proto.Services().ByName("AppStore").Methods()
	return &appStoreClient{
		ping: connect.NewClient[v1.PingRequest, v1.PingResponse](
			httpClient,
			baseURL+AppStorePingProcedure,
			connect.WithSchema(appStoreMethods.ByName("Ping")),
			connect.WithClientOptions(opts...),
		),
	}
}

// appStoreClient implements AppStoreClient.
type appStoreClient struct {
	ping *connect.Client[v1.PingRequest, v1.PingResponse]
}

// Ping calls api.v1.AppStore.Ping.
func (c *appStoreClient) Ping(ctx context.Context, req *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error) {
	return c.ping.CallUnary(ctx, req)
}

// AppStoreHandler is an implementation of the api.v1.AppStore service.
type AppStoreHandler interface {
	Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error)
}

// NewAppStoreHandler builds an HTTP handler from the service implementation. It returns the path on
// which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewAppStoreHandler(svc AppStoreHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	appStoreMethods := v1.File_api_v1_services_proto.Services().ByName("AppStore").Methods()
	appStorePingHandler := connect.NewUnaryHandler(
		AppStorePingProcedure,
		svc.Ping,
		connect.WithSchema(appStoreMethods.ByName("Ping")),
		connect.WithHandlerOptions(opts...),
	)
	return "/api.v1.AppStore/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case AppStorePingProcedure:
			appStorePingHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedAppStoreHandler returns CodeUnimplemented from all methods.
type UnimplementedAppStoreHandler struct{}

func (UnimplementedAppStoreHandler) Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("api.v1.AppStore.Ping is not implemented"))
}

// LocalStoreClient is a client for the api.v1.LocalStore service.
type LocalStoreClient interface {
	Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error)
}

// NewLocalStoreClient constructs a client for the api.v1.LocalStore service. By default, it uses
// the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewLocalStoreClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) LocalStoreClient {
	baseURL = strings.TrimRight(baseURL, "/")
	localStoreMethods := v1.File_api_v1_services_proto.Services().ByName("LocalStore").Methods()
	return &localStoreClient{
		ping: connect.NewClient[v1.PingRequest, v1.PingResponse](
			httpClient,
			baseURL+LocalStorePingProcedure,
			connect.WithSchema(localStoreMethods.ByName("Ping")),
			connect.WithClientOptions(opts...),
		),
	}
}

// localStoreClient implements LocalStoreClient.
type localStoreClient struct {
	ping *connect.Client[v1.PingRequest, v1.PingResponse]
}

// Ping calls api.v1.LocalStore.Ping.
func (c *localStoreClient) Ping(ctx context.Context, req *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error) {
	return c.ping.CallUnary(ctx, req)
}

// LocalStoreHandler is an implementation of the api.v1.LocalStore service.
type LocalStoreHandler interface {
	Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error)
}

// NewLocalStoreHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewLocalStoreHandler(svc LocalStoreHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	localStoreMethods := v1.File_api_v1_services_proto.Services().ByName("LocalStore").Methods()
	localStorePingHandler := connect.NewUnaryHandler(
		LocalStorePingProcedure,
		svc.Ping,
		connect.WithSchema(localStoreMethods.ByName("Ping")),
		connect.WithHandlerOptions(opts...),
	)
	return "/api.v1.LocalStore/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case LocalStorePingProcedure:
			localStorePingHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedLocalStoreHandler returns CodeUnimplemented from all methods.
type UnimplementedLocalStoreHandler struct{}

func (UnimplementedLocalStoreHandler) Ping(context.Context, *connect.Request[v1.PingRequest]) (*connect.Response[v1.PingResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("api.v1.LocalStore.Ping is not implemented"))
}
